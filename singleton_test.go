package idem

import (
	"testing"
)

func Test911_singleton_error(t *testing.T) {
	// init should have claimed it, so this should exit.
	// which it does. Good. So leave commented out.
	//singletonCheck_two_versions_of_idem_package_disallowed()
}
